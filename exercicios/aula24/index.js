/*
&& -> false && true -> false (valor)
|| -> true && false -> vai retonar `o valor verdadeiro`

    FALSY:
        false, 0, ``, null / undefined, NaN 
*/



/*console.log(0 || false || null || `Pedro` || true) // retonar pedro

const corUsuario = `vermelho`
const corPadrao = corUsuario || `preto`
console.log(corPadrao)*/

/*function falaOi () {
    return `oi`;
}

const vaiExecutar = `Pedro`;
console.log(vaiExecutar && falaOi()); // retorna a function*/

/*const vaiExecutar = false;
console.log(vaiExecutar && falaOi()); // retorna o false*/

/*console.log(`Luiz` && `Maria`) // retorna o ultimo valor vefiricado*/

/*console.log(`Pedro` && `` && `Maria`) // retorna o valor falso*/

/*console.log(`luiz otavio` && true && `Maria`)*/