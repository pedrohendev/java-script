// estrutura de repetição 

const frutas = [`Maçã`, `Pera`, `Uva`, `Luiz`, `Otavio`, `João`, `Miranda`];

for (let i = 0; i < frutas.length; i++) {
    console.log(`Índice ${i}`, frutas[i])
}


/*
// i = index
for (let i = 0; i <= 5; i++) {
    const par = i % 2 === 0 ? `par` : `impar`;
    console.log(`Linha ${i}, o número é ${par}`);
}
*/

